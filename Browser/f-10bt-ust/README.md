> [!NOTE]
> The site only contains the following text (with the encrypted/jumbled command) and a password box.

```html
<section>
    <div>Status: &lt;Waiting&gt;</div>
    <div>Encryption: &lt;No-Identity&gt;</div>
    lsh jffagkojt
    cqfuvnwg_gufe_srtqunll --scfnrdtuvk=
    <div>IP: &lt;No-Identity&gt;</div>
</section>
```

The first encoded line can be decoded with Vigenere with the key `radio` to 
```
    use broadcast
    zirdvkos_pucw_eatnmzul --purwralgek=
```

Using the Vigenere key `command` only on the second line gives
```
    activate_site_forcibly --parbfdgrtw=
```